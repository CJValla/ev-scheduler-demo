import React from 'react';
import ReactDOM from 'react-dom/client';
import EVScheduler from './EVScheduler.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EVScheduler />
  </React.StrictMode>
);
